apiVersion: v1
kind: Pod
metadata:
  name: postgres-pod
  labels:
    name: postgres-pod
    app: demo-voter-app
spec:
  containers:
    - name: redis
      image: redis
      ports:
       - containerPort: 5432